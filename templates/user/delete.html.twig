{% include 'default/_form.html.twig' with {
    page_title: 'title_user_delete'|trans({'%email%': user.email|default('')}),
    form: form,
    submit_label: 'action_delete'|trans,
    back_to_list_path: 'user_index',
} %}